name: CI

on:
  push:
    branches: [ "main", "desenvolvimento" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '7.0'

    - name: Restore
      run: dotnet restore

    - name: Build
      run: dotnet build ./ProjetoCiCd/ProjetoCiCd.csproj

    - name: Test
      run: dotnet test ./ProjetoCiCd.Tests/ProjetoCiCd.Tests.csproj